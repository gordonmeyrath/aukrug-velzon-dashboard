# Aukrug Workspace Environment Variables
# Copy this file to .env and fill in your actual tokens
# DO NOT commit real tokens to version control!

# Forgejo API token (scope: repo)
# Generate at: http://git.mioconnex.local/user/settings/applications
FORGEJO_TOKEN=__PUT_YOUR_FORGEJO_TOKEN_HERE__

# GitHub API token (scope: repo)  
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=__PUT_YOUR_GITHUB_TOKEN_HERE__

# Note: These tokens are ONLY used to auto-create remote repositories.
# All git push/pull operations use SSH keys.
# Make sure your SSH keys are registered on both Forgejo and GitHub.
